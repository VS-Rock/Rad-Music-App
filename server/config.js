const {
  GM_KEY, GM_SECRET, G_CLIENT_KEY, SEQUEL_PASS, TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, SEQUEL_HOST, SEQUEL_USER
} = process.env;

module.exports = {
  GM_KEY,
  GM_SECRET,
  G_CLIENT_KEY,
  SEQUEL_PASS,
  TWILIO_ACCOUNT_SID,
  TWILIO_AUTH_TOKEN,
  SEQUEL_HOST,
  SEQUEL_USER,
};
