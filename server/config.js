const {
  GM_SECRET, G_CLIENT_KEY, SEQUEL_PASS, TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, SEQUEL_HOST, SEQUEL_DATABASE, SEQUEL_USER,
} = process.env;

module.exports = {
  GM_SECRET,
  G_CLIENT_KEY,
  SEQUEL_PASS,
  TWILIO_ACCOUNT_SID,
  TWILIO_AUTH_TOKEN,
  SEQUEL_HOST,
  SEQUEL_USER,
  SEQUEL_DATABASE,
  SEQUEL_PASS,
};
